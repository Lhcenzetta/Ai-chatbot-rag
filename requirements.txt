# Backend
fastapi
uvicorn
jinja2
python-dotenv

# RAG stack
langchain
langchain-community
langchain-chroma
langchain-huggingface
langchain-text-splitters
langchain-ollama          # local LLM (default provider)
sentence-transformers     # multilingual embeddings
pypdf                     # PDF loading in ingest.py

# Optional: only needed when LLM_PROVIDER=api (OpenAI-compatible endpoints)
openai
